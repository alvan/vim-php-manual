trader_set_compat -- Set compatibility mode

  void trader_set_compat(integer compatId)~

Set compatibility mode which will affect the way calculations are done by all
the extension functions.

Compatibility Id. TRADER_COMPATIBILITY_* series of constants should be used.

No value is returned.

vim:ft=help:
