trader_get_compat -- Get compatibility mode

  integer trader_get_compat()~

Get compatibility mode which affects the way calculations are done by all the
extension functions.

Returns the compatibility mode id which can be identified by
TRADER_COMPATIBILITY_* series of constants.

vim:ft=help:
