trader_floor -- Vector Floor

  array trader_floor(array real)~

Calculates the next lowest integer for each value in {real} and returns the
resulting array.

Array of real values.

Returns an array with calculated data or false on failure.

vim:ft=help:
