fann_get_sarprop_step_error_threshold_factor -- Returns the sarprop step error
threshold factor

  float fann_get_sarprop_step_error_threshold_factor(resource ann)~

The sarprop step error threshold factor.

The default factor is 0.1.

Neural network resource.

The sarprop step error threshold factor, or FALSE on error.

|fann_set_sarprop_step_error_threshold_factor|

vim:ft=help:
