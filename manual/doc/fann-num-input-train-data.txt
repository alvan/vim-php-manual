fann_num_input_train_data -- Returns the number of inputs in each of the
training patterns in the train data

  int fann_num_input_train_data(resource data)~

Returns the number of inputs in each of the training patterns in the train
data resource.

Neural network training data resource.

The number of inputs, or FALSE on error.

|fann_length_train_data| |fann_num_output_train_data|

vim:ft=help:
